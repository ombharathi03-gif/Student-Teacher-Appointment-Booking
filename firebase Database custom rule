rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read/write only to logged-in users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
